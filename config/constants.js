const path = require("path")

const PROJECT_ROOT = path.resolve(".")
const SEEDER_DIR = "seed"
const DOMAINS_FILENAME = "domains.csv"

module.exports = {
  SEEDER_DIR, DOMAINS_FILENAME, PROJECT_ROOT
}
